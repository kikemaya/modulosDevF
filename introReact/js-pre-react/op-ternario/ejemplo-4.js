// Uso en una expresión para realizar una operación matemática condicional:

const numero = 10;
const resultado = numero > 5 ? numero * 2 : numero + 2;
console.log(resultado); // Resultado: 20

// En este ejemplo, se realiza una operación matemática diferente en función de si numero es mayor que 5 o no.