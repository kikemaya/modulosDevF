// Uso en una expresión de concatenación de strings:

const estaSoleado = true;
const clima = estaSoleado ? 'soleado' : 'nublado';
console.log(`El clima está ${clima}.`); // Resultado: 'El clima está soleado.'

// En este caso, el operador ternario se utiliza para determinar el valor de la variable clima, que luego se utiliza en una cadena de texto interpolada.

// El operador ternario es una herramienta útil para escribir condicionales de manera más concisa y legible en JavaScript.